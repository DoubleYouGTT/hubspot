#' @param  offset Used to page through the results. If there are more records in your portal than
#' the limit= parameter, you will need to use the offset returned in the first request to get the
#' next set of results.
